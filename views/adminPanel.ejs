<!DOCTYPE html>
<html lang="pl-PL">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Panel</title> 
    <link rel="stylesheet" href="/adminPanel.css" type="text/css">
</head>
<body>
    <div class="container">
        <form class="form" method="POST">
            <input type="text" placeholder="Login" name="login" required="true">
            <input type="text" placeholder="Hasło" name="password" required="true">
            <button type="submit">Wyślij</button>
        </form>
        <div class="row">
            <div class="dataID data">ID</div>
            <div class="data">Login</div>
            <div class="data">Hasło</div>
            <div class="data">Ilość Kroków</div>
            <div class="data">Operacje</div>
        </div>
            <% if(test.length > 0) {%>
                <% for(var i = 0 ; i <test.length ; i++){%>
            <div class="row">
                <div class="data"><%= test[i].id%></div>
                <div class="data"><%= test[i].login%></div>
                <div class="data"><%= test[i].password%></div>
                <div class="data"><%= test[i].step_count%></div>
                <div class="operations">
                    <button class="updateButton"><a href=""></a>Update</button>
                    <a href="./delete?id=<%= test[i].id %>"><button class="deleteButton">Delete</button></a>
                </div>
            </div>
        <% } %>
                <% } else { %>
                    <div class="notFound">
                        <h1>Nie znaleziono danych</h1>
                    </div>
                <% } %>
    </div>
</body>
</html>